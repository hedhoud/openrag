"""Tests for the upcoming Postgres PartitionMembershipRepository.

Reserved placeholder — real tests land once Person A's 7A.2
``partition_membership_repo.py`` implementation merges. Colocated with
the implementation (project convention). The port ABC is expected to
land in the same commit (no ``partition_membership_repo.py`` exists
under ``openrag/core/ports/`` yet).
"""

import pytest

pytestmark = pytest.mark.skip(reason="awaiting 7A.2 partition_membership_repo + port from Person A")


def test_placeholder() -> None:
    pass
