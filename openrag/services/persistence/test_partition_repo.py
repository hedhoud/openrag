"""Tests for the upcoming Postgres PartitionRepository.

Reserved placeholder — real tests land once Person A's 7A.2
``partition_repo.py`` implementation merges. Colocated with the
implementation (project convention) and will exercise the
``PartitionRepository`` ABC (see ``openrag/core/ports/partition_repo.py``).
"""

import pytest

pytestmark = pytest.mark.skip(reason="awaiting 7A.2 partition_repo from Person A")


def test_placeholder() -> None:
    pass
