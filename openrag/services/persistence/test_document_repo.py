"""Tests for the upcoming Postgres DocumentRepository.

Reserved placeholder — real tests land once Person A's 7A.2
``document_repo.py`` implementation merges. Colocated with the
implementation (project convention) and will exercise the
``DocumentRepository`` ABC (see ``openrag/core/ports/document_repo.py``).
"""

import pytest

pytestmark = pytest.mark.skip(reason="awaiting 7A.2 document_repo from Person A")


def test_placeholder() -> None:
    pass
