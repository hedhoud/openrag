"""Tests for the upcoming Postgres UserRepository.

Reserved placeholder — real tests land once Person A's 7A.2
``user_repo.py`` implementation merges. Colocated with the
implementation (project convention) and will exercise the
``UserRepository`` ABC (see ``openrag/core/ports/user_repo.py``).
"""

import pytest

pytestmark = pytest.mark.skip(reason="awaiting 7A.2 user_repo from Person A")


def test_placeholder() -> None:
    pass
